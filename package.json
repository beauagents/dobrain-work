{
  "name": "dobrain-work",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight Cloudflare Worker starter for a polyrepo deployment setup",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
