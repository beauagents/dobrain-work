name: LLM Changelog (Codex)

on:
  push:
    branches:
      - main
    paths-ignore:
      - '*.md'
      - '.github/**'
  workflow_dispatch:

jobs:
  changelog:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 10
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Install Codex CLI
        run: npm install -g @openai/codex
      - name: Generate changelog
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          CODEX_MODEL: ${{ vars.CODEX_CHANGELOG_MODEL || 'gpt-4o' }}
        run: |
          COMMITS=$(git log --oneline -10 --no-merges)
          codex --quiet --model "$CODEX_MODEL" "Generate a concise changelog entry from these commits. Group by type (features, fixes, maintenance). Use markdown. Focus ONLY on changelog. Commits: $COMMITS" > /tmp/changelog.md 2>&1 || true
      - name: Commit changelog
        run: |
          ENTRY=$(cat /tmp/changelog.md)
          DATE=$(date +%Y-%m-%d)
          if [ -f CHANGELOG.md ]; then
            EXISTING=$(cat CHANGELOG.md)
            echo "# Changelog"$'\n\n'"## $DATE"$'\n\n'"$ENTRY"$'\n'"${EXISTING#*# Changelog}" > CHANGELOG.md
          else
            echo "# Changelog"$'\n\n'"## $DATE"$'\n\n'"$ENTRY" > CHANGELOG.md
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add CHANGELOG.md
          git diff --cached --quiet || git commit -m "docs: update changelog [skip ci]" && git push
